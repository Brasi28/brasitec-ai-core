# Funciones Disponibles - Autonomous Agent

- `runAgent(agentId, projectPath, goal, context?)`
- `executeCommand(command, cwd?)`
- `analyzeProject(rootPath)`
- `queryGithubExamples(query, language?, maxExamples?)`
